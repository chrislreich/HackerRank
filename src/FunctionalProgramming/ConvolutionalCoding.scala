package FunctionalProgramming

/**
  * Created by creich on 5/18/18.
  */



object ConvolutionalCoding {
/*
  class Encoder(val n : Int, val k : Int, val polynomials : List[List[String]]) {
    val m_cells = Array[Int](7)
    for (i <- 0 until 7) {
      m_cells(i) = 0
    }

    val t_sequences : List[List[Int]] = for (p <- polynomials) yield p.zipWithIndex.filter(_._1 == "1").map(_._2).reverse

    private def updateArray(s : Int) : Unit = {
      for (i <- 5 until -1 by -1) {
        m_cells(i + 1) = m_cells(i)
      }
      m_cells(0) = s
    }

    def transmit(s : Int) : String
  }

  def run = {
    val line1 = readLine().split(" ").map(_.toInt)
    val n = line1(0)
    val k = line1(1)



  }

  def main(args : Array[String]) : Unit = {

  }
*/
}
